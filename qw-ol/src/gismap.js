

export function makeGis(conf) {
    const appNode = document.getElementById("app");
    const testNode = document.createElement('div');
    testNode.innerHTML = `<h1>TESTing add node to app...</h1>`
    appNode.appendChild(testNode);
    return true;

}

